const express = require('express');
const filmCtrl = require('./games.controller');
const router = express.Router();

// router.post('/', gamesCtrl.newFilm);
// router.get('/', gamesCtrl.getAll);
// router.get('/:id', gamesCtrl.getOne);
// router.put('/:id', gamesCtrl.upsert);
// router.delete('/:id', gamesCtrl.deleteFilm);

module.exports = router;